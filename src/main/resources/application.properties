spring.application.name=aesty-messenger

# --- Настройки БД ---
# Используй ${JDBC_DATABASE_URL} на Render, локально используй свой localhost
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:postgresql://localhost:5433/aesty_messenger_db}
spring.datasource.username=${JDBC_DATABASE_USERNAME:postgres}
spring.datasource.password=${JDBC_DATABASE_PASSWORD:BimaAriec1562@}
spring.datasource.driver-class-name=org.postgresql.Driver

# --- Настройки Hibernate ---
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create

# --- Настройки сессий ---
spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=always
spring.session.jdbc.table-name=SPRING_SESSION

# --- Безопасность (JWT) ---
jwt.secret=${JWT_SECRET:A71E47478999780BE24B9E99EB94DF3FFC734BE72F9C4844067D31F339BF74D4}
jwt.expiration=604800

# --- Настройки почты ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME:kutbaranov@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:nhha lnls kysc xigp}
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
app.email.url=${APP_EMAIL_URL:http://localhost:8080/confirm-account}

# --- Google OAuth2 ---
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile,email

# --- Прочее ---
spring.thymeleaf.cache=false